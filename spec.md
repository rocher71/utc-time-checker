# APZ Alliance Time Checker — 확정 스펙

Whiteout Survival APZ 연맹의 이벤트 발동시간 결정 도구.
UTC 슬라이더를 움직이면 연맹원 타임존별 현지 시간이 한눈에 보인다.

## 결정 사항

### 플랫폼 / 배포
- 정적 단일 페이지: `index.html` 하나 (vanilla HTML/CSS/JS, 빌드·프레임워크 없음)
- GitHub Pages 배포: `rocher71/utc-time-checker` main 브랜치 루트
- 모바일 우선 레이아웃 (연맹원 대부분 폰으로 접속 전제)

### UI 언어
- **영어** — Nabubu, Kelz 등 비한국어권 연맹원 공유 전제

### 데이터 관리
- `index.html` 안 JS 배열 하나를 직접 수정 후 git push로 갱신
- 스키마:
  ```js
  const ZONES = [
    { label: "KST", offset: 9,  members: ["Sooya", "유준빠"] },
    { label: "BST", offset: 1,  members: ["Nabubu"] },
    { label: "MST", offset: -7, members: ["Kelz"] },
  ];
  ```
- 타임존 조사가 끝나는 대로 항목 추가

### 타임존 처리
- **고정 오프셋** (UTC+9 / +1 / −7). IANA·DST 자동 반영은 제외 (사용자 선택)
- 알려진 한계: 영국이 서머타임을 벗어나면(10월 말) BST → GMT로 offset을 0으로,
  미국 서머타임 시작 시 MST → MDT −6으로 수동 수정 필요. 파일 주석으로 안내

### 슬라이더
- 범위 UTC 00:00–24:00, **30분 단위** (48스텝)
- 초기값: 페이지 로드 시점의 현재 UTC를 30분으로 반올림
- **Now 버튼**으로 현재 시각 복귀, 슬라이더 트랙 위에 현재 시각 마커 표시

### 타임존별 표시 (슬라이더 아래 카드/행)
- 타임존 라벨 + 오프셋, 현지 시간 (HH:MM), 날짜가 다르면 `+1d` / `−1d` 배지
- 소속 연맹원 닉네임 나열
- **2단계 주야 색상**: 현지 08:00–24:00 = 깨어있음(초록 계열), 00:00–08:00 = 수면(회색/어두움)
- 슬라이드하며 "전부 초록"인 지점을 찾는 것이 사용 흐름

## 제외한 대안 (근거)
- 웹 UI에서 멤버 추가: GitHub Pages 정적 호스팅이라 공유 저장이 안 됨(localStorage는 기기별).
  수정 주체가 한 명이므로 파일 수정으로 충분
- IANA 타임존 + 자동 DST: 사용자가 고정 오프셋 선택 (단순함 우선, 연 2회 수동 수정 감수)
- 3단계(활동/애매/수면) 색상: 2단계로 충분하다고 판단
- 최적 시간 자동 추천: 색상만으로 판단 가능, YAGNI

## 완료 기준
- [ ] 폰 화면에서 슬라이더 조작 시 모든 타임존 시간이 즉시 갱신
- [ ] 30분 스텝, 초기값 = 현재 UTC, Now 버튼 동작
- [ ] 수면 시간대 타임존이 시각적으로 구분됨
- [ ] ZONES 배열에 항목 추가만으로 새 타임존 반영
- [ ] https://rocher71.github.io/utc-time-checker/ 에서 접속 가능
